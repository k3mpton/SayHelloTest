syntax = "proto3"; 

package chat; 

option go_package = "github.com/legiseen/chatHelloPro;lega";

service Chat { 
    rpc SayHello(Message) returns (Reply) {}
}

message Message { 
    string body = 1; 
}

message Reply { 
    string HelloName = 1;
}
